function Dashboard() {
  return (
    <section>
      <h1>Dashboard</h1>
      <p>
        News feed, filters and search go here. The UI talks to the backend via{' '}
        <code>/api/…</code> (proxied by nginx).
      </p>
    </section>
  )
}

export default Dashboard
