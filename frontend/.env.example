# Frontend env. Only VITE_-prefixed vars are exposed to the client at build time.
# Do NOT put secrets here — everything here ships to the browser.

# Base path for API calls. Same-origin by default: nginx proxies /api to the
# backend, so the app can call relative `/api/...` with no host.
VITE_API_BASE_URL=/api
