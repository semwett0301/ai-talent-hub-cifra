# domain

Pure State Duma bill snapshots, plain-language change values, and the tracking State
pattern. `state/` transitions `tracking` to terminal `published`; `unsupported` keeps
legacy rows out of the monitor. `model/` contains the per-article change value.
