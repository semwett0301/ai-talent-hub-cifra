# errors

Typed duplicate, URL/page validation, source/document transport, and OpenRouter model
failures. HTTP routes map validation to 422, duplicates to 409, and remote failures to 502;
the daily monitor logs per-act source/model failures and retries on its next run.
