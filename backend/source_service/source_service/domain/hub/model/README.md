# domain/hub/model

- `origin.py` — `HubOrigin`: where a hub came from — `listing` (confirmed by the LLM
  classifier), `seed` (the home page when nothing else was found).

Notes: the origin is provenance, not a quality signal — a `seed` hub is scored like any
other.
