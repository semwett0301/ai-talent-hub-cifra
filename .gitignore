# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.eggs/
dist/
build/

# Env & secrets
.env
*.session          # Telethon session files
*.session-journal

# IDE
.idea/
.vscode/
.DS_Store

# Tooling
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/

# Data / runtime
*.log
data/*.db
crawl4ai_cache/

# Local-only working docs: plans/ = what we agreed to build, outcome/ = what was built
plans/
outcome/

# Personal compose overrides (the shared dev overlay is docker-compose.dev.yml)
docker-compose.override.yml

# Dozzle login (bcrypt hashes) — created by hand next to .env, never committed
dozzle/users.yml
